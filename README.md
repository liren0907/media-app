# Media App

A modern media analysis application built with Tauri (v2), Rust, and SvelteKit. This tool provides a user-friendly interface for video playback, RTSP stream ingestion, and real-time HLS streaming capabilities.

### Development

To run the application in development mode:

```bash
# Install dependencies
yarn install

# Start the Tauri application
yarn tauri dev
```
